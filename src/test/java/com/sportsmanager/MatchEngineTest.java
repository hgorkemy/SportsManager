package com.sportsmanager;

import org.junit.jupiter.api.Test;

/**
 * TODO (Berke): Write 5 meaningful tests for FootballMatchEngine.
 * Suggested:
 * - hasNextPeriod() returns true before first period
 * - hasNextPeriod() returns false after 2nd period
 * - simulateNextPeriod() returns non-null SegmentResult
 * - scores in SegmentResult are non-negative
 * - simulateFullMatch() returns non-null MatchResult
 */
class MatchEngineTest {

    @Test
    void placeholder() {
        // TODO (Berke): Replace with real tests
    }
}
